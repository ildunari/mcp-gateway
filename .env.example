# OAuth Configuration
# Generate a secure token with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
GATEWAY_AUTH_TOKEN=CHANGE_ME_TO_SECURE_RANDOM_TOKEN

# Server Configuration
PORT=4242
NODE_ENV=development

# Production URL (for production deployment)
# NODE_ENV=production