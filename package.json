{
  "name": "mcp-gateway",
  "version": "1.0.0",
  "description": "MCP Gateway for hosting multiple MCP servers accessible from Claude.ai",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development node --watch src/server.js",
    "dev:windows": "set NODE_ENV=development && node --watch src/server.js",
    "test": "NODE_ENV=development node src/server.js"
  },
  "keywords": ["mcp", "gateway", "claude"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.1.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "cors": "^2.8.5",
    "winston": "^3.11.0",
    "dotenv": "^16.3.1"
  }
}